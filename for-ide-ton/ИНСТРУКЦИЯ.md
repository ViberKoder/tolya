# 📋 Инструкция по деплою Jetton 2.0 через ide.ton.org

## Шаг 1: Открой ide.ton.org

Перейди на https://ide.ton.org

---

## Шаг 2: Сначала задеплой Jetton Wallet

1. Создай новый файл или очисти редактор
2. Скопируй **весь** код из файла `jetton-wallet-full.fc`
3. Нажми **Compile** (компиляция)
4. Сохрани полученный **BOC код** — он понадобится для деплоя Minter

> ⚠️ **ВАЖНО:** Wallet деплоится автоматически при минте токенов, НО его код нужен для Minter!

---

## Шаг 3: Задеплой Jetton Minter

1. Очисти редактор
2. Скопируй **весь** код из файла `jetton-minter-full.fc`
3. Нажми **Compile**
4. Нажми **Deploy**

### При деплое нужно указать начальные данные (Initial Data):

```
total_supply: 0
admin_address: <твой адрес кошелька>
content: <cell с метаданными - см. ниже>
jetton_wallet_code: <BOC код wallet из шага 2>
```

---

## Шаг 4: Создание Content Cell (метаданные токена)

Для создания метаданных используй этот формат:

### Вариант 1: Off-chain метаданные (проще)

Content cell должен содержать:
```
0x01 + URI строку
```

Например для IPFS:
```
0x01 + "ipfs://QmYourHash/metadata.json"
```

### Вариант 2: On-chain метаданные

Используй словарь с ключами (SHA256):
- `name` → название токена
- `symbol` → символ
- `decimals` → количество знаков (обычно "9")
- `description` → описание
- `image` → URL картинки

---

## Шаг 5: Минт токенов

После деплоя Minter, отправь сообщение с op = 21 (mint):

```
op: 21 (0x15)
query_id: 0
to_address: <адрес получателя>
jetton_amount: <количество в nano, например 1000000000000000 = 1M токенов>
forward_ton_amount: 10000000 (0.01 TON)
total_ton_amount: 50000000 (0.05 TON)
```

---

## 📁 Файлы

| Файл | Описание |
|------|----------|
| `jetton-minter-full.fc` | Мастер-контракт (деплоить этот) |
| `jetton-wallet-full.fc` | Контракт кошелька (код нужен для minter) |

---

## 🔑 Важные моменты

1. **Admin** — адрес который указан при деплое, только он может минтить
2. **Decimals = 9** — 1 токен = 1,000,000,000 nano-токенов
3. **Для минта 1 миллиона токенов** отправь: `1000000 * 10^9 = 1000000000000000`

---

## 💡 Альтернатива

Если ide.ton.org сложно использовать, попробуй:
- **TON Web IDE**: https://tact-by-example.org
- **Tonkeeper Wallet** + dApp для деплоя
- **Blueprint** (командная строка): `npx blueprint run deployJettonMinter --testnet`
